{
    "service": {
        "name": "postgres_exporter",
        "port": 9187,
        "tags": ["prometheus"],
        "checks": [
            {
                "tcp": "{{ ansible_host }}:9187",
                "interval": "5s",
                "timeout": "2s"
            }
        ]
    }
}
