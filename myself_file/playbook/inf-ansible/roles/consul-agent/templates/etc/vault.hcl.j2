{
    "service": {
        "name": "vault",
        "port": 8200,
        "checks": [
            {
                "tcp": "{{ ansible_host }}:8200",
                "interval": "5s",
                "timeout": "1s"
            }
        ]
    }
}